# Base URL of the backend server (no trailing slash).
# Used for both the Socket.IO connection and the REST API.
# In production, set this to your deployed server URL, e.g.:
# VITE_BACKEND_URL=https://your-server.onrender.com
VITE_BACKEND_URL=http://localhost:3001
