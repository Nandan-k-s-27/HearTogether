# Port for the Express server
PORT=3001

# Allowed frontend origin (no trailing slash)
FRONTEND_URL=http://localhost:5173

# WebRTC NAT traversal is handled by free public Open Relay TURN servers.
# No TURN credentials are required — the server serves them automatically.
